CREATE TABLE IF NOT EXISTS USERS (
    ID LONG GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FIRST_NAME VARCHAR(255) NOT NULL,
    LAST_NAME VARCHAR(255) NOT NULL,
    EMAIL VARCHAR(255),
    PHONE_NUMBER VARCHAR(50),
    ADDRESS VARCHAR(255)
);




CREATE TABLE IF NOT EXISTS DEVICES (
    ID LONG GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR(255) NOT NULL,
    DEVICE_TYPE VARCHAR(50), 
    POWER_RATING INTEGER,
    IS_ON BOOLEAN,
    DTYPE VARCHAR(20), 
    
    OPERATING_SYSTEM VARCHAR(100),
    IP_ADDRESS VARCHAR(50),
    CONNECTIVITY_STRENGTH INTEGER,
    
    NUMBER_OF_KNOBS INTEGER,
    ENERGY_CLASS CHAR(1),
    
    FOREIGN KEY (USER_ID) REFERENCES USERS(ID)
);





CREATE TABLE IF NOT EXISTS MEASUREMENTS (
    ID LONG GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID LONG NOT NULL,
    DEVICE_ID LONG NOT NULL,
    MEASUREMENT_DATE DATE NOT NULL,
    MEASUREMENT_TYPE VARCHAR(50) NOT NULL,
    MEASUREMENT_VALUE DECIMAL(15, 2) NOT NULL,
    UNIT VARCHAR(20) NOT NULL,
    
    FOREIGN KEY (USER_ID) REFERENCES USERS(ID),
    FOREIGN KEY (DEVICE_ID) REFERENCES DEVICES(ID)
);




CREATE TABLE IF NOT EXISTS APP_USERS (
    ID LONG GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USERNAME VARCHAR(50) UNIQUE NOT NULL,
    PASSWORD VARCHAR(255) NOT NULL  
);


INSERT INTO APP_USERS (USERNAME, PASSWORD) VALUES ('admin', 'admin123');
INSERT INTO APP_USERS (USERNAME, PASSWORD) VALUES ('user', 'user123');




